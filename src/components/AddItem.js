export default function AddItem() {
  // when i click the button
  // it should add another item

  return (
    <button
      className="add-item"
      id="add-item"
      type="button"
    >
      Add Item
    </button>
  );
}
